#include <stdio.h>
#include <math.h>

double log2_factorial(int n) {
    double sum = 0;
    for (int i = 1; i <= n; i++)
        sum += log2(i);
    return sum;
}

int main() {
    double keys_power = log2_factorial(100);
    printf("Possible keys ˜ 2^%.2f\n", keys_power);
    return 0;
}
